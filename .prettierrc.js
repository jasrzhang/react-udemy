module.exports = {
  semi: true, // Adds a semicolon at the end of every statement.
  trailingComma: "es5", // Adds trailing commas wherever possible (ES5+).
  singleQuote: true, // Use single quotes instead of double quotes.
  printWidth: 80, // Specifies the line length that the printer will wrap on.
  tabWidth: 2, // Specifies the number of spaces per indentation level.
  jsxSingleQuote: false, // Use double quotes in JSX attributes.
  bracketSpacing: true, // Print spaces between brackets in object literals.
  arrowParens: "avoid", // Omit parens when possible. Example: `x => x`.
  jsxBracketSameLine: false, // Puts the `>` of a multi-line JSX element at the end of the last line instead of being alone on the next line.
  endOfLine: "lf", // Use line feed only (\n) as line ending.
};
